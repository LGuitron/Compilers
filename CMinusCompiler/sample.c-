int global;
int globalarr[2];

int suma(int a, int b)
{
    return a+b;
}

int globalfunc(void)
{
    global = 5;    
    return global;
}

void main(void)
{
    int a;
    int b;
    
    a = 4;
    output(suma(3, 7));
    b = 3;
    output(a+b);
    
    
    output(globalfunc());
    
    globalarr[1] = 4;
    
    global = 4;
    globalarr[1] = 17;
    output(global);
    output(globalarr[1]);
}
