1. Fix problems with function return
2. Fix function return values (with if and else)
3. Generate code for while
4. Generate code for input

----------

Optional:

1. Limit array upper bound when array is passed as parameter
2. Initialize global arrays with 0s
